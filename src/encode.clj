(defn encode-qoi [bytes width height channels]
    (let [  prevColor   (list 0 0 256)
            run-length  0           
            lut         (hash-map)  ;; look up table of seen values
            ] 

        ;; returns bytes
        ))


(defn do-encode [bytes width height channels]
    ;; recursive helper function

    ;; returns bytes
    )

